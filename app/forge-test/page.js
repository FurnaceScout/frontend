import ForgeTestRunner from "@/app/components/ForgeTestRunner";

export const metadata = {
  title: "Forge Test Runner - FurnaceScout",
  description: "Run and manage Forge tests with detailed results",
};

export default function ForgeTestPage() {
  return <ForgeTestRunner />;
}

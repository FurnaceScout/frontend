# FurnaceScout Docker Environment Configuration
# Copy this file to .env.docker and adjust as needed

# =============================================================================
# RPC Configuration
# =============================================================================

# RPC endpoint for blockchain connection
# For Anvil running on host machine (default):
NEXT_PUBLIC_RPC_URL=http://host.docker.internal:8545

# For Anvil running in Docker compose (uncomment if using):
# NEXT_PUBLIC_RPC_URL=http://anvil:8545

# For remote RPC endpoint:
# NEXT_PUBLIC_RPC_URL=https://your-rpc-endpoint.com

# =============================================================================
# Application Configuration
# =============================================================================

# Node environment
NODE_ENV=production

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# Application port (internal container port)
PORT=3000

# Hostname binding
HOSTNAME=0.0.0.0

# =============================================================================
# Foundry Project Path
# =============================================================================

# Path to Foundry project inside container (mounted via docker-compose.yml)
# This is where FurnaceScout will look for broadcast/ and out/ directories
FOUNDRY_PROJECT_PATH=/foundry-project

# =============================================================================
# Optional: Wallet Configuration
# =============================================================================

# If you want to enable wallet features in Docker
# NEXT_PUBLIC_ENABLE_WALLET=true

# =============================================================================
# Optional: Polling Intervals (milliseconds)
# =============================================================================

# Block polling interval
# NEXT_PUBLIC_BLOCK_POLL_INTERVAL=12000

# Transaction polling interval
# NEXT_PUBLIC_TX_POLL_INTERVAL=5000

# =============================================================================
# Optional: Feature Flags
# =============================================================================

# Enable/disable specific features
# NEXT_PUBLIC_ENABLE_FOUNDRY_SCAN=true
# NEXT_PUBLIC_ENABLE_CONTRACT_INTERACTION=true
# NEXT_PUBLIC_ENABLE_ANVIL_CONTROLS=true

# =============================================================================
# Docker-Specific Settings
# =============================================================================

# Health check configuration (already set in Dockerfile)
# HEALTHCHECK_INTERVAL=30s
# HEALTHCHECK_TIMEOUT=10s
# HEALTHCHECK_RETRIES=3

# =============================================================================
# Notes
# =============================================================================
#
# 1. host.docker.internal:
#    This special DNS name allows containers to access services running on
#    the host machine. Works on Docker Desktop (Mac/Windows) automatically.
#    On Linux, add --add-host=host.docker.internal:host-gateway to docker run.
#
# 2. Volume mounting:
#    Mount your Foundry project to /foundry-project in the container.
#    Example: -v /path/to/your/foundry/project:/foundry-project:ro
#
# 3. Network modes:
#    - Bridge (default): Containers can communicate via service names
#    - Host: Container shares host network (direct access to host ports)
#
# 4. Security:
#    The container runs as non-root user (nextjs:nodejs)
#    Foundry project is mounted read-only (:ro) for safety
#
